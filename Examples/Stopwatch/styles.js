import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    app: {
        height: '100%',
        alignItems: 'center',
        backgroundColor: 'black'
    },
    timer__container: {
        marginTop: 100
    },
    timer: {
        color: '#ccc',
        fontWeight: '200',
        fontSize: 70
    }
});