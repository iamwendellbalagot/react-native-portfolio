import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    app: {
        height: '100%',
        backgroundColor: '#d4e2d4',
        alignItems: 'center'
    },
    app__title: {
        fontSize: 40,
        fontWeight: 'bold',
        color: '#433d3c',
        paddingVertical: 20,
        marginTop: 30
    }
});